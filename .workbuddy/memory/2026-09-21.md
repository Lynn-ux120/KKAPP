# 2026-09-21

## 《坤坤大逃亡》pygame 游戏 → Android APK

### 环境结论（重要，别再重复排查）
- Buildozer / python-for-android **只能在 Linux 跑**，这台 Windows 机器走不通本地打包：
  - `wsl.exe` 被安全策略程序黑名单拦截（无法通过）
  - 未安装 Docker
  - 无 JDK / ANDROID_HOME / JAVA_HOME
  - 无 gh CLI、无 adb
- Git Credential Manager **已安装**（`credential.helper = manager`），用户自己 push 会弹窗登录，不需要手动 token。
- GitHub 用户名：`Lynn-ux120`；本机 git 全局 user 已配置为 Lynn-ux120 / 189308256@qq.com。

### 已确定的打包方案：GitHub Actions 云构建
- 工作流：`.github/workflows/build-apk.yml`（ubuntu-22.04 + Python 3.11 + JDK 17）
- 产物：Actions → Artifacts → `kunkundaotao-apk`

### 四个必须锁死的版本（原配置缺这些必挂）
1. `requirements = python3==3.10.13,pygame` —— p4a 的 pygame 配方是 **2.1.0**，只支持到 Python 3.10；
   而 p4a 现在默认 Python 已是 **3.14.2**。不锁死必挂。
2. `Cython<3.0` —— Cython 3.x 移除 `longintrepr.h`，会编崩 pygame/kivy 配方。
3. `android.api = 33` + `android.ndk = 25b` —— NDK r25b 最高只支持 API 33。
4. `android.accept_sdk_license = True` —— 否则 CI 卡在许可证确认直到超时。

### 工程结构改动
- `kunkun_da_tao_wang.py` = 真正的游戏主程序（ASCII 文件名，buildozer 要求）
- `KK大逃亡（点我运行）.py` = 电脑端双击启动器（仅 3 行 import）
- `main.py` = Android 入口
- `tools/smoke_test.py` 无头冒烟测试（跑 300 帧 + 三界面渲染，输出 SMOKE OK）
- `tools/gen_icon.py` 生成 icon.png（512×512，24 位 RGB）
- `font.otf`（16MB）已内置，解决安卓中文方块问题
- `dsh-deep-whale/` 是无关前端工程，已在 `source.exclude_dirs` 排除，但仍在 git 跟踪中

### 当前阻塞
- 目标仓库 `https://github.com/Lynn-ux120/KKAPP` **不存在**（GitHub API 404，该账号无任何公开仓库），
  本地那个 `first commit` 从来没推上去过。
- 等用户创建空仓库后 push 即可触发构建。

### 本地验证
- pygame-ce 2.5.8（Python 3.13.11）可跑冒烟测试
- 游戏已加 `pygame.SCALED`：720×820 逻辑分辨率等比铺满手机屏，触摸坐标自动换算
